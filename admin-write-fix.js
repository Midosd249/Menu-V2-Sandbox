/* Deprecated: canonical product save is admin.js #saveItem only.
 * Kept as a no-op so any stale loader cannot override the verified path.
 */
(function(){
  console.info('[Menu Admin] admin-write-fix.js is intentionally inert; save path is admin.js');
})();
