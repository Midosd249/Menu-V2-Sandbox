# قائمة إطلاق أول عميل مدفوع — Menu V2

استخدم هذه القائمة قبل تفعيل منيو أي مطعم مدفوع. كل بند يجب أن يُنفَّذ ويُؤكَّد.

## أ. إعداد المستأجر (Tenant)

1. [ ] إنشاء سجل في جدول `tenants` (slug فريد، الاسم، الألوان، واتساب، شعار).
2. [ ] إنشاء فرع واحد على الأقل في `branches` (`is_active = true`).
3. [ ] إنشاء تصنيفات في `categories` مرتبة بـ `sort_order`.
4. [ ] إدخال الأصناف في `products` مع أسعار معتمدة وصور موافق عليها.

## ب. الحساب والصلاحيات

5. [ ] إنشاء مستخدم المالك في **Supabase Auth** (البريد الرسمي للمطعم).
6. [ ] إدراج صف في `tenant_members`:
   - `tenant_id` = معرّف النشاط
   - `user_id` = UUID من Auth
   - `role` = `owner`
7. [ ] (اختياري) حساب `editor` لموظف المنيو بنفس الطريقة مع `role = editor`.
8. [ ] تسجيل الدخول من `admin.html` والتأكد من ظهور **بيانات حية** وليس «وضع العرض».

## ج. الأمان والتخزين

9. [ ] تطبيق migration: `supabase/migrations/20260831_role_hardening_and_event_guard.sql` من SQL Editor.
10. [ ] التحقق من سياسات Storage لـ bucket `menu-assets` (قراءة عامة، كتابة بمسار tenant فقط).
11. [ ] تجربة رفع صورة صنف واحدة والتأكد أنها تظهر في المنيو العام.
12. [ ] التأكد أن `supabase-config.js` على النشر يحتوي **anon key فقط**.

## د. تجربة العميل

13. [ ] فتح رابط المنيو من هاتف حقيقي عبر QR أو `/m/{slug}/{branch}`.
14. [ ] التحقق من RTL، البحث، التفاصيل، واتساب، وحالة «غير متوفر».
15. [ ] تعديل سعر أو توفر من لوحة الإدارة ثم تحديث صفحة المنيو العام.
16. [ ] تسجيل الخروج ثم التأكد أن أزرار الحفظ معطّلة.

## هـ. النطاق والتشغيل

17. [ ] ربط النطاق / HTTPS على Vercel (أو الاستضافة المستخدمة).
18. [ ] التحقق من Security Headers في استجابة الإنتاج.
19. [ ] نسخة احتياطية سريعة (Export للجداول أو snapshot).
20. [ ] تحديد قناة دعم العميل (واتساب/بريد) ووقت الاستجابة المتوقع.

## و. التسليم

21. [ ] إرسال رابط المنيو + رابط لوحة الإدارة + بيانات الدخول للمالك فقط عبر قناة آمنة.
22. [ ] حذف أي بيانات تجريبية غير معتمدة من حساب العميل.
23. [ ] توثيق slug والفرع وتاريخ الإطلاق داخليًا.
